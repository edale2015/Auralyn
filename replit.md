# Auralyn — HIPAA/FDA Medical Triage Platform (Multi-Tenant SaaS)

### Overview
Auralyn is an AI-powered medical triage platform for flu-like symptoms, leveraging WhatsApp for initial patient assessments. It provides diagnoses and treatment plans for physician review, automates patient communication, and enhances healthcare efficiency and access. The system is designed for continuous improvement through a self-developing AI architecture, with a vision to transform medical triage into a more efficient, patient-centric process. Key capabilities include a unified EHR write architecture, advanced diagnostic engines, and comprehensive security and compliance features. The platform also features extensive command center dashboards for monitoring, predictive analytics, and digital twin simulations, supporting multi-hospital and regional deployments.

### User Preferences
Preferred communication style: Simple, everyday language.

### System Architecture

#### Core Architecture
The system employs a constrained agent architecture with a plan/act/observe loop and a multi-system triage pipeline. It features a unified sheets registry for data configuration, a clinical state builder, and a modular, skill-based orchestration layer. An intelligence layer provides explainability and failure-driven rule suggestions, with an extended learning loop for continuous improvement. The Clinical Intelligence Planning Layer (CIPL) and Clinical Governance & Deployment Layer ensure continuous improvement and safe deployment of clinical changes. The system adheres to a 12-layer architecture encompassing Interface, Normalization, State, Knowledge, Safety, Reasoning, Decision, Learning, Analytics, Governance, Integration, and Orchestration. All clinical EHR writes flow through a single, guarded pipeline ensuring authentication, scope checks, and audit logging. A robust validation system, including golden cases, adversarial generators, and calibration monitors, enforces strict deployment gates. The system incorporates a Medical Command and Control (MCP) Nervous System, allowing for dynamic registration and execution of clinical tools and workflows, with an immutable audit hash chain.

#### UI/UX Decisions
The frontend is built with React 18, TypeScript, `shadcn/ui`, and Tailwind CSS, offering intuitive interfaces for physicians, patients, and administrators. Key dashboards include: Clinical Simulation Lab, Clinical Control Tower, Executive Dashboard, Stress Test Dashboard, Patient Queue Dashboard, FDA Validation Dashboard, Decision Tree Explorer, Live Clinic Console, Production Readiness Console, Clinical QA Dashboard, Clinical Improvement Lab, Care Pathway Optimizer, Skill Graph, Revenue War Room, Governance Command Center, Physician Command Strip, Clinical Brain Dashboard, System Validation Dashboard, Clinical Operations Center, Clinical Brain Page, Agent System Page, Cognitive Brain Page, Communication Dashboard, Master Control, Hospital Dashboard, Live Patient Monitor, and Command Center v2/v3/v4.

#### Technical Implementations
The backend uses Express 5, Node.js, and TypeScript, providing REST API endpoints. It incorporates Centor score calculation, red flag detection, a supervisor gate, and robust LLM integrations. Features include: Clinical Brain Engine (rewritten to v3.0 phase-parallel), Self-Developing Medical AI, Telemedicine Reasoning Assistant, and an Agent System for patient flow. Clinical capabilities include advanced triage logic, medication safety layers, FHIR-lite structured output, Clinical Knowledge Graph, 30+ Complaint Packs with a Visual Rule Builder, Adaptive Control Loop, Case Memory Engine, Unified Clinical Pipeline, Autonomous Operator System, Engine Control Center, Auto-Debug Engine, Agent Coordinator, SMS/WhatsApp Service, Stress Test System, RPA Browser Agent, FDA Submission Package, Live Patient Queue, Predictive Failure Engine, Autonomous Loop, Safety Gate, Immutable Audit Logger, and Explainability integrated into the orchestrator. The system uses a Multi-Agent Debate Engine, Continuous Learning Pipeline with RLHF, and Policy Evolution Engine. All clinical decisions are managed via Postgres KB tables. Core intelligence utilities include Fisher Information Matrix, Natural Gradient, Bayesian Updater, and Hallucination Guards. Security is hardened with AES-256-GCM encryption for credentials, HMAC-SHA validation for Twilio webhooks, and comprehensive PHI guarding before external API calls. Multi-tenant isolation is enforced at various layers. A deep agent Python sidecar provides advanced AI capabilities like research, code review, and workflow upgrades, integrated via a TypeScript bridge. Communication intelligence engines detect patient tone, generate communication scripts, and manage antibiotic demand responses. The system features a unified EHR adapter interface for ECW, Athena, and Epic, with a 5-tier fallback write chain. It also includes an Agent Scope Engine (ASE) for risk-based, role-aware action governance.

#### System Design Choices
Data management uses Firebase Firestore, SQLite, and PostgreSQL, with PHI retention policies. Authentication involves password-only, session-based HMAC for physicians and token-based access for patients, with JWT-based role authentication. Security and quality hardening include bcrypt, JWT security, rate limiting, and PHI Sanitizer. A Global SRE + Resilience Layer provides geo-aware routing, SLA monitoring, automatic debugging, and chaos engineering. Autonomous Governance includes an agent registry, audit agent, incident commander, digital twin, and predictive engine. The Autonomous Operator System is an AI-powered form automation engine. A Template Studio allows visual template editing. The Replay Inspector audits automation runs. A Robotics Control Module manages medical device orchestration. An Autonomous Learning Console provides a unified dashboard for self-testing, self-learning, and governance. The Multi-Patient Command Grid provides a three-pane, hospital-style dashboard with risk-sorted patient grids. The system is designed for multi-region deployment with auto-scaling capabilities and a unified control API. It includes a medical knowledge graph, DAG visualizer, and YAML pipeline engine for complex workflow execution.

### External Dependencies
*   **AI Integration**: OpenAI API (GPT-4o-mini, GPT-4o, GPT-4-turbo, LangChain, LangGraph)
*   **Messaging Integration**: Twilio (WhatsApp, SMS, Voice TTS), Telegram
*   **Database**: PostgreSQL, Firebase Firestore, SQLite, Redis (BullMQ for queuing)
*   **Data Configuration**: Google Sheets
*   **Cloud Storage**: Firebase Storage, AWS S3
*   **Authentication**: Google OAuth2
*   **EHR Systems**: Epic (FHIR + SMART on FHIR skeleton), Athena Health (proprietary REST), eClinicalWorks (SMART on FHIR complete)
*   **Win 9 (FHIR/EHR Context Layer)**: `server/integrations/ehr/fhirPatientContext.ts` (unified PatientContext fetcher — ecw/athena/epic/mock), `epicConnector.ts` (SMART auth skeleton), `intakePrePopulationService.ts` (EHR pre-population of answers.structured), `priorAuthSkeleton.ts` (CMS-style prior auth assessment), `server/routes/fhirContext.routes.ts` (registered at `/api/ehr/*`), `client/src/components/PatientContextPanel.tsx` (physician-triggered EHR context panel in CaseReview). Key API: `GET /api/ehr/context/:patientId?vendor=mock|ecw|epic|athena`, `POST /api/ehr/prior-auth`, `POST /api/ehr/prepopulate`. Auth: `x-review-token` header (requireReviewAuth).
*   **Win 10 (Command Orchestration)**: `server/routes/command.routes.ts` (POST `/api/command`, OpenAI gpt-4o-mini intent parser, 10 intent categories), `client/src/components/AuralynCommandInterface.tsx` (floating `⌘K` button, global, listens for `auralyn:open-command` custom event).
*   **Win 11 (Clinical AI Harness)**: `AGENTS.md` (repo root — governance rules, safety caps, golden principles GP-01 through GP-06, state machine skeleton, drift canary registry), `server/harness/driftCheck.ts` (20 frozen canonical canary cases + `runDriftCheck()` runner — wire into BullMQ daily at 2am UTC), `server/harness/harnessEnforcer.ts` (Addition 2: `enforceAgentCaps()` + `HarnessCapExceeded` error class; Addition 3: `buildClinicalContext()` async context injector with EHR merge and GP-05 precedence; `createAgentState()` factory; `incrementStep/LlmCall/ToolRetry` mutators; `HARNESS_CAPS` constants). Caps: 5 reasoning steps, 2 tool retries, 8 LLM calls, $1.50 per case. Audit event `SAFETY_CAP_EXCEEDED` fires on any breach.
*   **Monitoring**: Prometheus, Grafana, LangSmith
*   **Version Control**: GitHub
*   **External APIs**: NCBI (PubMed), ClinicalTrials.gov, Payer APIs
*   **Win 17a (LLM Gateway — Bifrost Architecture)**: `server/gateway/llmGateway.ts` — singleton `llmGateway` with `complete()` + `cacheStats()`. 7 ModelPurpose types (clinical_brain, retrieval_pruner, uncertainty_sampler, intent_parser, kb_validator, skill_generator, discharge_generator) + `cme_quiz`. Each purpose maps to a primary/fallback provider+model pair: Opus for clinical_brain/kb_validator/skill_generator, Sonnet for everything else; OpenAI gpt-4o / gpt-4o-mini as fallback (uses `AI_INTEGRATIONS_OPENAI_API_KEY`/`AI_INTEGRATIONS_OPENAI_BASE_URL` or `OPENAI_API_KEY`). **Semantic cache**: in-memory LRU 500 entries, 4-hour TTL, SHA-256 keyed by `purpose:cacheKey`. **Automatic failover**: primary failure logs LLM_GATEWAY_FAILOVER audit event and retries fallback provider; zero call-site changes needed. **Audit**: every non-cached call emits `LLM_GATEWAY_CALL` audit event with purpose, model, provider, tokensUsed, estimatedCostUsd, latencyMs.
*   **Win 17b (Three-Tier Clinical KB Retriever)**: `server/retrieval/clinicalKBRetriever.ts` — `retrieveClinicalKB(complaintSlug, symptoms, ehrContext?)` → `RetrievalResult`. Three-hop retrieval loop: Hop 1 — parallel fetch of red_flag rules + diagnosis/disposition/treatment rules + active clinical_skills from Postgres; Hop 2 — if token count >6000, runs Claude Sonnet pruner to identify irrelevant chunks and remove them (aggressive: "if it wouldn't change the clinical assessment, prune it"); Hop 3 — if budget allows, fetches adjacent complaint red flags (hardcoded adjacency map: chest_pain↔SOB, sore_throat↔ear_pain, etc.). Budget: 8000 token hard cap, 6000 soft threshold. Returns `curatedBlock` (structured prompt section with pruning stats, chunk counts, hop count) ready for direct injection into `runClinicalBrain()` context. `KBChunk` type with id/type/complaintSlug/content/tokenEstimate.
*   **Win 22 (Pathway Completion Engine + Physician Review Dashboard)**: **DB**: `pathway_drafts` table (slug UNIQUE, draft_json JSONB, review_items JSONB, validation_score, status, approved_by, rejection_reason) + `clinical_pathways` table (slug UNIQUE, pathway_json JSONB, system, acuity_class, approved_by, version, active); 6 indexes. **Engine**: `server/clinical/pathwayCompletionEngine.ts` — `completePathwayDraft(request)` fires 3 async LLM sub-drafters: `draftLRTables` (Opus/kb_validator — drafts likelihood ratio tables for each DDx from published decision rules, cites sources, sets mustNotMiss=false for physician review), `draftPhysicalExam` (Sonnet/retrieval_pruner — required/conditional exam components + clinical findings), `draftPatientCommunication` (Sonnet/discharge_generator — diagnosis explanation, treatment explanation, 3+ return precautions, followUp, prevention, NPS drivers). Also generates a `draftDriftCanary` (typical test case). Validates draft with `validatePathway()`, saves to `pathway_drafts` with `pending_physician_review` status. Never auto-loads to KB — physician approval required. `runBatchCompletion(slugs)` batch-processes with 3s rate limiting. **5 new API routes** (all requireReviewAuth + requireRole physician/admin): `POST /api/clinical/pathways/complete-draft` (fires async, returns immediately), `GET /api/clinical/pathways/pending-review` (DB query ordered by score DESC, limit 50; registered BEFORE /:slug to avoid route capture), `POST /api/clinical/pathways/:slug/approve` (score≥80 gate, loads to clinical_pathways via INSERT ON CONFLICT, emits CLINICAL_PATHWAY_APPROVED audit), `POST /api/clinical/pathways/:slug/reject` (emits CLINICAL_PATHWAY_REJECTED audit), **Frontend**: `client/src/pages/PhysicianPathwayReview.tsx` (route `/pathway-review`, admin+physician) — 3-tab dashboard: Coverage Map (23-system CoverageBar progress visualization), Pending Review (PathwayReviewCard — expandable, shows review checklist, DDx preview with must-not-miss flags, return precautions preview, Approve≥80/RequestRevision/Reject actions), P1 Gaps (list with Generate Draft button per gap). Nav item "Pathway Review" + ClipboardCheck icon added.
*   **Win 21 (Complaint Pathway Schema + Sheet Migrator + ENT/Eye/Infectious Pathways)**: `server/clinical/complaintPathwaySchema.ts` — complete TypeScript interface for `ComplaintPathway` covering 10 sub-schemas (RedFlagRule, IntakeQuestion, DifferentialDiagnosis with LR tables, PhysicalExam, WorkupProtocol, DispositionCriteria, TreatmentProtocol, PatientCommunication, FollowUpProtocol) + `validatePathway()` function scoring 0-100 with error/warning differentiation. `server/clinical/sheetMigrator.ts` — `SheetMigrator` class with `fromCSVString(content)` / `fromCSV(path)` / `saveMigrationOutput()`: 14-column alias normalization (50+ header variants), CSV parser with quoted-field support, 8 field parsers (red flags, questions, differential, physical exam, workup, treatment, disposition, patient communication), MigrationReport with readyForProduction/needsReview/criticalGaps arrays, physician review checklist generator. `server/clinical/PATHWAY_MASTER_MAP.ts` — 23-system, 214-complaint taxonomy: 3 complete (✅), 23 partial (🟡), 188 missing (❌); 155 critical-if-missed; exports `ALL_SYSTEMS`, `MASTER_SUMMARY`, `getComplaintsByPriority()`, `getCriticalMissingP1()`, `getSystemStatus()`. `server/clinical/pathways/ent-eye-infectious.ts` — 5 complaint pathways: `earPainPathway` (COMPLETE — 5 red flags, 12 intake questions, 6 DDx with LR tables, full physical exam, workup, treatment, patient communication, follow-up; guidelines: AAP 2022, AAO-HNS 2017, IDSA), `eyeComplaintPathway`/`fluCovidPathway`/`skinInfectionPathway`/`dentalPainPathway` (full red flags + ERdisposition stubs, 5-6 red flags each including ER_IMMEDIATE triggers for ACG, necrotizing fasciitis, Ludwig's angina, CRAO). `server/routes/clinicalPathways.routes.ts` — 7 endpoints (all requireReviewAuth + requireRole admin/physician): `GET /api/clinical/pathways/master-map`, `GET /api/clinical/pathways`, `GET /api/clinical/pathways/:slug`, `POST /api/clinical/pathways/validate`, `POST /api/clinical/pathways/load` (admin, requires score≥80+no errors), `POST /api/clinical/pathways/migrate-csv`, `GET /api/clinical/pathways/system/:systemName`, `GET /api/clinical/pathways/priority/:level`. Router wired in `server/index.ts` line 914 as `app.use(clinicalPathwaysRouter)`.
*   **Win 17c (Physician CME Quiz Tool)**: `client/src/pages/PhysicianCMEQuizTool.tsx` (route `/cme-quiz`, physician+admin). 6 quiz topics: Red Flag Recognition, Centor/HEART/Wells, Drug Safety & Interactions, Disposition Decisions, Pediatric Urgent Care, Feynman Explainer. Topic selection screen → adaptive chat session. **Score tracking**: heuristic pattern match on assistant response (correct/well done → streak++, incorrect/not quite → streak reset); streak badge at ≥1, mastery badge at ≥10 consecutive. **Chat**: POST /api/cme/chat → `llmGateway.complete()` with `cme_quiz` purpose (Claude Sonnet, 800 max tokens, no cache for live quiz). System prompt enforces: never give answer first, concise coach not lecturer, escalating difficulty, 10-streak goal, evidence-grounded (ACEP/AAP/AHA/CDC). DESIGN.md tokens used throughout (bg-clinical, bg-routine-light, bg-knowledge-light, rounded-full badges). Nav item "CME Quiz" with GraduationCap icon added.
*   **Win 16 (Self-Healing Infrastructure Monitor)**: `server/infra/selfHealingMonitor.ts` (SelfHealingMonitor singleton + `recordSchedulerHeartbeat` export). Monitors 6 critical services via `runCycle()` every 5 minutes (initial check 30s after start): (1) `checkPostgresPool` — `SELECT 1` latency probe, healthy <3s, degraded ≥3s, down on error; (2) `checkBullMQWorker` — attempts `createDurableQueue("followup")`, falls back to graceful degraded, counts recent FOLLOW_UP_MESSAGE_SENT audit events; (3) `checkWebSocketGateway` — counts GEOMETRIC_REASONING_INJECTED/HARNESS_CONTEXT_INJECTED events in last 4h, degrades during business hours if 0; (4-6) `checkScheduler` dead-man's switch for drift_canary (26h window), research_radar (170h/weekly), skill_nudge (26h window) — "unknown" until first heartbeat recorded. On failure: `diagnoseFailure` calls Claude (claude-sonnet-4-20250514) with last 2h audit events as context; `remediatePostgres` runs probe query; `remediateBullMQ` calls `registerFollowUpWorker()`; `remediateScheduler` calls registered re-arm fn. `handleFailure` emits SELF_HEAL_SUCCEEDED/SELF_HEAL_FAILED audit events, logs human-intervention escalation if remediation fails. Scheduler heartbeats: `recordSchedulerHeartbeat("drift_canary"|"research_radar"|"skill_nudge")` added to each scheduler's fire callback in `server/index.ts`. Re-arm functions registered via `SelfHealingMonitor.registerSchedulerRearm(service, fn)` at startup. **API**: `GET /api/infra/status` (requireReviewAuth) — returns `{ services, lastRunAt, allHealthy, incidents[] }`. **Frontend**: `client/src/pages/InfraStatusDashboard.tsx` (route `/infra-status`, admin only) — overall status banner (green/yellow/red), service grid with latency/heartbeat/failure-count details, recent self-healing incident list with auto-resolved vs requires-human labels; nav item "Infrastructure" added to AppLayout with Activity icon.
*   **Win 15 (Hermes-Inspired Clinical Skills System + Spec-Driven Development)**: `server/learning/clinicalSkillsSystem.ts` (4-function module: `generateSkillFromOverrides` calls Claude to codify physician override patterns into ClinicalSkill docs — minimum 3 overrides, max 0.95 confidence; `retrieveRelevantSkills` queries active skills from Postgres by complaintSlug and builds a Tier 1 memory injection block capped at ~800 tokens; `activateSkill` / `retireSkill` — physician-gated approval/retirement with CLINICAL_SKILL_ACTIVATED/CLINICAL_SKILL_RETIRED audit events; `runPeriodicSkillNudge` — nightly analysis of audit_hash_chain for complaint slugs with ≥20% override rate in last 30 days, generates new skills, skips if active/pending skill exists in last 60 days). `server/harness/specDrivenDevelopment.ts` (SpecDrivenDevelopment object + `specRouter` Router: `createSpec` generates 6-section spec from plain-English goal using Claude with embedded Auralyn tech stack; `completeTask` / `propagateBackward` / `blockTask` for task lifecycle; specs persist as JSON + spec.md + tasks.md in `.specs/`; specRouter registers 5 routes at `/api/harness/spec*`). DB table `clinical_skills` created via psql (skill_id UNIQUE, complaint_slug, title, trigger_text, ai_tendency, correct_reasoning, evidence_basis, confidence, override_count, status, version, activated_at). **Wired in pipeline.ts**: after HARNESS_CONTEXT_INJECTED / before runGeometricReasoning — retrieves up to 3 active skills for the case's `_ont.complaintSlug`, appends to `systemPromptAdditions`, fires SKILLS_INJECTED trace event. **Wired in server/index.ts**: specRouter mounted with `app.use`, 3 CRUD routes (`GET /api/clinical-skills`, `POST /api/clinical-skills/:id/activate`, `POST /api/clinical-skills/:id/retire`), `scheduleSkillNudge()` armed for nightly 3am UTC. **Frontend**: `client/src/pages/ClinicalSkillsDashboard.tsx` (route `/clinical-skills`, physician + admin roles) — shows pending-review skills with Activate/Reject buttons, active skills with Retire option, spec tracker with per-spec progress bars; nav item added to AppLayout under Research Radar with Brain icon. Loop: Override → audit_hash_chain → nudge → Claude evaluation → pending_review → physician activates → retrieved into system prompt → better triage → fewer overrides.
*   **Win 14 (Clinical Ontology Layer)**: `server/ontology/clinicalOntology.ts` (DISPOSITION_ONTOLOGY + COMPLAINT_ONTOLOGY + resolveDisposition/resolveComplaint + validateCaseOntology + OntologyValidationError + `ont` singleton), `server/ontology/ontologyFieldMapper.ts` (OntologyFieldMapper class — enrichCaseDoc, disposition/complaint resolution helpers, semanticPassport; standalone helpers: returnPrecautionsKey, canonicalDisposition, canonicalComplaintSlug), `server/ontology/ontologyFirewall.ts` (4 production gates: guardIntake/guardTriageOutput/guardDischarge/guardFollowUpEnrollment). **Wired in `server/agent/pipeline.ts`**: Gate 1 before CLINICAL_BRAIN_ENGINE (enriches all pipeline state with `_ont` canonical fields, fires ONTOLOGY_GATE1_PASSED trace event), Gate 2 after CLINICAL_BRAIN_COMPLETE (blocks red-flag+SELF_CARE violations, forces URGENT_CARE safe default, fires ONTOLOGY_TRIAGE_BLOCKED/ONTOLOGY_TRIAGE_WARNINGS events). **Wired in `server/routes/review.routes.ts`**: Gate 3 before discharge sendWhatsAppMessage (blocks un-physicianized discharge, fires DISCHARGE_ONTOLOGY_BLOCKED audit event), Gate 4 before enrollInFollowUp (blocks enrollment if phone missing or disposition not follow-up eligible), review queue GET handler enriches every case with `_ont` via OntologyFieldMapper.enrichCaseDoc so frontend CaseSnapshotCard receives canonical disposition/complaint/urgency/async-safe/eConsult fields without per-component DISPOSITION_MAPs.
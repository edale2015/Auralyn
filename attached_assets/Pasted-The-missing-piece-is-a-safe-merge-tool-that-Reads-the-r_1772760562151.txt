The missing piece is a safe merge tool that:

Reads the review bundle.

Adds only .new.csv rows into the production tables.

Optionally merges selected conflict rows.

Creates backups of the production CSVs before modifying them.

Remains idempotent.

Below is a first draft of that script.

scripts/merge-approved-drafts.ts
/**
 * scripts/merge-approved-drafts.ts
 *
 * Safely merge emitted draft rows into production CSV tables.
 *
 * Reads:
 *   data/complaints/review/<complaint_id>/*.new.csv
 *
 * Writes:
 *   server/data/csv/*.csv
 *
 * Safety features:
 * - full backup of target CSVs
 * - idempotent (skips duplicate rows)
 * - dry-run mode
 */

import fs from "fs";
import path from "path";

type Args = {
  complaintId: string;
  dryRun: boolean;
};

function parseArgs(argv: string[]): Args {
  const complaintId = argv[0];
  if (!complaintId) {
    console.error("Usage: npx tsx scripts/merge-approved-drafts.ts <complaint_id> [--dry-run]");
    process.exit(2);
  }
  return { complaintId, dryRun: argv.includes("--dry-run") };
}

function splitCsvLine(line: string): string[] {
  const out: string[] = [];
  let cur = "";
  let inQuotes = false;

  for (let i = 0; i < line.length; i++) {
    const ch = line[i];
    if (ch === '"') {
      if (inQuotes && line[i + 1] === '"') {
        cur += '"';
        i++;
      } else inQuotes = !inQuotes;
      continue;
    }

    if (ch === "," && !inQuotes) {
      out.push(cur);
      cur = "";
      continue;
    }

    cur += ch;
  }

  out.push(cur);
  return out.map((s) => s.trim());
}

function readCsv(filePath: string) {
  if (!fs.existsSync(filePath)) return { headers: [], rows: [] as Record<string, string>[] };

  const raw = fs.readFileSync(filePath, "utf8");
  const lines = raw.split(/\r?\n/).filter((l) => l.trim().length > 0);

  if (!lines.length) return { headers: [], rows: [] };

  const headers = splitCsvLine(lines[0]);
  const rows: Record<string, string>[] = [];

  for (let i = 1; i < lines.length; i++) {
    const cols = splitCsvLine(lines[i]);
    const r: Record<string, string> = {};
    for (let j = 0; j < headers.length; j++) r[headers[j]] = cols[j] ?? "";
    rows.push(r);
  }

  return { headers, rows };
}

function writeCsv(filePath: string, headers: string[], rows: Record<string, string>[]) {
  const lines: string[] = [];
  lines.push(headers.join(","));

  for (const r of rows) {
    const cols = headers.map((h) => {
      const v = r[h] ?? "";
      if (v.includes(",") || v.includes('"')) return `"${v.replace(/"/g, '""')}"`;
      return v;
    });
    lines.push(cols.join(","));
  }

  fs.writeFileSync(filePath, lines.join("\n") + "\n", "utf8");
}

function backupFile(filePath: string) {
  const ts = new Date().toISOString().replace(/[:.]/g, "-");
  const backupPath = `${filePath}.bak.${ts}`;
  fs.copyFileSync(filePath, backupPath);
  return backupPath;
}

function main() {
  const args = parseArgs(process.argv.slice(2));
  const root = process.cwd();

  const reviewDir = path.join(root, "data", "complaints", "review", args.complaintId);
  const liveDir = path.join(root, "server", "data", "csv");

  if (!fs.existsSync(reviewDir)) {
    throw new Error(`Review bundle not found: ${reviewDir}`);
  }

  const files = fs.readdirSync(reviewDir).filter((f) => f.endsWith(".new.csv"));

  let totalAdded = 0;

  for (const f of files) {
    const table = f.replace(".new.csv", "");
    const draftPath = path.join(reviewDir, f);
    const livePath = path.join(liveDir, `${table}.csv`);

    const draft = readCsv(draftPath);
    const live = readCsv(livePath);

    if (!draft.headers.length) continue;

    const mergedHeaders = [...new Set([...live.headers, ...draft.headers])];

    const liveSet = new Set(
      live.rows.map((r) => JSON.stringify(mergedHeaders.map((h) => r[h] ?? "")))
    );

    const toAdd: Record<string, string>[] = [];

    for (const row of draft.rows) {
      const norm = mergedHeaders.map((h) => row[h] ?? "");
      const key = JSON.stringify(norm);

      if (!liveSet.has(key)) {
        const newRow: Record<string, string> = {};
        mergedHeaders.forEach((h, i) => (newRow[h] = norm[i]));
        toAdd.push(newRow);
      }
    }

    if (toAdd.length === 0) {
      console.log(`${table}: nothing new`);
      continue;
    }

    console.log(`${table}: ${toAdd.length} new rows`);

    if (!args.dryRun) {
      const backup = backupFile(livePath);
      console.log(`backup: ${backup}`);

      const mergedRows = [...live.rows, ...toAdd];
      writeCsv(livePath, mergedHeaders, mergedRows);
    }

    totalAdded += toAdd.length;
  }

  console.log(`\nMerge complete. Rows added: ${totalAdded}`);
}

main();
Example workflow with the new tool
compile-guideline-to-ir.ts
normalize-ir.ts
emit-ir-to-csvs.ts
review-emitted-drafts.ts
merge-approved-drafts.ts

Example:

npx tsx scripts/compile-guideline-to-ir.ts sore_throat data/complaints/sources/sore_throat.txt
npx tsx scripts/normalize-ir.ts sore_throat
npx tsx scripts/emit-ir-to-csvs.ts sore_throat
npx tsx scripts/review-emitted-drafts.ts sore_throat
npx tsx scripts/merge-approved-drafts.ts sore_throat --dry-run

Then finally:

npx tsx scripts/merge-approved-drafts.ts sore_throat
npm run abi:complaints
npm test
What this completes

You now have a full compiler pipeline:

guideline text
  ↓
IR compiler
  ↓
normalized IR
  ↓
CSV emitter
  ↓
review bundle
  ↓
safe merge
  ↓
engine rules

That’s the missing automation layer that lets your system scale from tens of complaints to hundreds without hand-writing every rule.
1) Fix the 6 tie-based failures with DX_PRIORITY.csv

Append these rows to server/data/csv/DX_PRIORITY.csv (or wherever you store it):

Complaint_Slug,Dx_ID,Priority
derm_cellulitis,CL_DCELL_ABSCESS,80
derm_cellulitis,CL_DCELL_NEC_FAS,70

ent_epistaxis,CL_EPIX_POSTERIOR,80
ent_epistaxis,CL_EPIX_ANTERIOR,70

gi_diarrhea,CL_DIARR_INVASIVE_BACTERIAL,80
gi_diarrhea,CL_DIARR_VIRAL_GASTRO,70

gi_dysphagia,CL_DYSPH_IMPACTION,80
gi_dysphagia,CL_DYSPH_MALIGNANCY,60

gu_urinary_retention,CL_URET_NEUROGENIC,80
gu_urinary_retention,CL_URET_BPH_OBSTRUCTION,70

Why these priorities make sense:

Cellulitis: abscess beats nec_fas when tied at low scores (prevents “worst label wins by alphabet”).

Epistaxis: posterior wins ties (conservative, higher risk).

Diarrhea: invasive bacterial wins tie with viral when equal evidence (conservative).

Dysphagia: impaction wins tie over malignancy in acute presentation set (your canon).

Retention: neurogenic wins tie over BPH when equal (safer).

After adding, run:

npm run ship:complaint -- derm_cellulitis
npm run ship:complaint -- ent_epistaxis
npm run ship:complaint -- gi_diarrhea
npm run ship:complaint -- gi_dysphagia
npm run ship:complaint -- gu_urinary_retention
npm run harness

That should clear C9, G02, G09, GID7, GIS9, GUR4 purely via deterministic priority.

2) Fix the 3 non-tie failures with surgical micro-packs

These go in micro_packs/<complaint>.csv and then you run build + ship.

A) GUF3 — gu_flank_pain: nephrolithiasis beating pyelonephritis (score 4 vs 2)

You want pyelo to win when fever present (classic). Minimal adjustment: boost pyelo on fever, and suppress stone when fever.

Complaint_Slug,Dx_ID,Rule_ID,Logic,Points,Pack_Version,Notes
gu_flank_pain,pyelonephritis,BOOST_PYELO_FEVER_V1,"fever=true",2,v1,"Fever strongly favors pyelo"
gu_flank_pain,nephrolithiasis,NEG_STONE_FEVER_V1,"fever=true",-3,v1,"Fever suppresses uncomplicated stone"
B) ND5 — neuro_dizziness_vertigo: BPPV beating vestibular neuritis (2 vs 1, margin 1)

ND5 is typically “spinning + vomiting” which should lean neuritis rather than BPPV. Minimal: boost neuritis when vomiting, and slightly suppress BPPV when vomiting.

Complaint_Slug,Dx_ID,Rule_ID,Logic,Points,Pack_Version,Notes
neuro_dizziness_vertigo,vestibular_neuritis,BOOST_VN_VOMIT_V1,"vomit=true",2,v1,"Vomiting favors neuritis"
neuro_dizziness_vertigo,bppv,NEG_BPPV_VOMIT_V1,"vomit=true",-2,v1,"Vomiting suppresses BPPV"
C) G04 — pulm_chest_tightness: pleurisy beating asthma (5 vs 4, margin 1)

You want asthma to win when wheeze present. Minimal: boost asthma if wheeze, suppress pleurisy if wheeze.

Complaint_Slug,Dx_ID,Rule_ID,Logic,Points,Pack_Version,Notes
pulm_chest_tightness,asthma,BOOST_ASTHMA_WHEEZE_V1,"wheeze=true",2,v1,"Wheeze favors asthma/reactive airway"
pulm_chest_tightness,pleurisy,NEG_PLR_WHEEZE_V1,"wheeze=true",-2,v1,"Wheeze suppresses pleurisy winner"
Run these 3

Put each block into its complaint micro pack file:

micro_packs/gu_flank_pain.csv

micro_packs/neuro_dizziness_vertigo.csv

micro_packs/pulm_chest_tightness.csv

Run:

npx tsx scripts/build-micro-packs.ts
HARNESS_MODE=1 npx tsx scripts/tune-pairs.ts
npm run harness
Expected outcome

The 6 tie failures become deterministic and disappear.

The 3 scoring mismatches resolve with 2 rules each.

You should land at 1247/1247 PASS (unless there’s an additional hidden tie revealed after priorities change).

If anything still fails afterward, paste the updated “clean summary” like you just did (tie vs non-tie), and we’ll repeat.
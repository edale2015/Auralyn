1) Change CLINICIAN_PASSWORD now

Do this immediately. Use a strong random value (20–30 chars). Example pattern:

6–8 words + separators + a couple digits/symbols

or a password manager generated string

Rotate it monthly (your ops guide already notes this).

2) Add two “cheap safety rails” (high ROI)

Even without CSRF tokens, these two are worth it:

A) Rate limit sensitive endpoints

/api/auth/login (prevent brute force)

/api/intake/:token/verify (prevent guessing codes)

/api/intake/:token/upload (prevent abuse/cost spikes)

If you don’t want Redis, an in-memory limiter is fine on Replit for now.

B) Upload limits + file type allowlist

You likely already cap size; also restrict MIME types:

images: jpeg/png/webp

pdf
Block executables.

3) Confirm “cookie + proxy” behavior on Replit public URL

Since everything is on port 5000 (same origin), you’re in the happy path. Still, do one live check:

Open your public URL in an incognito window

Login

Reload dashboard

Confirm you stay logged in (cookie sticks)

If you ever move API to a different origin, cookie settings change (SameSite=None; Secure), but you don’t need that now.

4) Optional CSRF token: good to add when you have 20 minutes

You don’t have to do it today, but your design is ready for it.

Best minimal version:

issue a csrf cookie on login (not httpOnly)

require X-CSRF header on POST/DELETE /api/provider/*

compare header to cookie

5) Next strategic workstream choices

Now that infrastructure is stable, you can pick what gives you the most clinical leverage:

A) Provider dashboard improvements

Filters: urgent flags, attachments, submitted today

One-click “Mark in review,” “Sign,” “Close”

Quick note editor before copy/export

B) eCW Stage 2 (when they reply)

SMART launch + patient context pull

attach intake packet + note as DocumentReference

keep prescribing/billing inside eCW

C) Clinical safety layer

Medication interaction checks (if you’re drafting meds)

Red flag escalation rules

Automated “go to ER” messaging when certain symptom combos are checked

“You are live” checklist (final)

Before you use it with real patients:

✅ Firestore = on

✅ Firebase Storage = on

✅ Bucket = medicalm-dec9d.firebasestorage.app

✅ Provider auth cookie works on public URL

✅ CLINICIAN_PASSWORD rotated to strong secret

✅ Provider key fallback disabled

✅ Basic rate limits (recommended)